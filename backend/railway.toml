[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/*.jar"
healthcheckPath = "/api/vehiculos"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"