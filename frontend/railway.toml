[build]
builder = "NIXPACKS"

[build.buildCommand]
cmd = "npm install && npm run build"

[deploy]
startCommand = "npx serve dist -p ${PORT:-3000}"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"